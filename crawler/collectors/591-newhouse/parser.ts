import type { Raw591NewHouseListing, NewHouseCollectorResult } from './types';
export function parseNewHouseItems(payload: unknown): Raw591NewHouseListing[] { const envelope = payload && typeof payload === 'object' && !Array.isArray(payload) ? payload as Record<string, unknown> : undefined; const data = envelope?.data && typeof envelope.data === 'object' ? envelope.data as Record<string, unknown> : undefined; const items = Array.isArray(payload) ? payload : data?.items; if (!Array.isArray(items)) throw new Error('SOURCE_CHANGED: expected newhouse data.items array'); return items.map((item) => { if (!item || typeof item !== 'object') throw new Error('PARSE_ERROR: invalid newhouse item'); const raw = item as Record<string, unknown>; return { projectId: stringValue(raw.projectId ?? raw.id ?? raw.project_id ?? raw.hid), projectName: stringValue(raw.projectName ?? raw.name ?? raw.build_name), projectType: stringValue(raw.projectType ?? raw.type ?? raw.build_type_name), city: stringValue(raw.city ?? raw.region), district: stringValue(raw.district ?? raw.section), priceText: stringValue(raw.priceText ?? raw.price), unitPriceText: stringValue(raw.unitPriceText ?? raw.price_unit), areaText: stringValue(raw.areaText ?? raw.area), roomText: stringValue(raw.roomText ?? raw.rooms ?? raw.room), mrtText: stringValue(raw.mrtText ?? raw.mrt), addressText: stringValue(raw.addressText ?? raw.address), sourceUrl: stringValue(raw.sourceUrl ?? raw.url ?? (raw.hid ? `https://newhouse.591.com.tw/${raw.hid}` : undefined)), rawStatus: stringValue(raw.status), raw }; }); }
function stringValue(value: unknown): string | undefined { return typeof value === 'string' || typeof value === 'number' ? String(value) : undefined; }
export function parseNewHousePage(payload: unknown, page: number): NewHouseCollectorResult { try { const items = parseNewHouseItems(payload); return { status: 'SUCCESS', items, fetchedPages: page, warnings: [], errors: [] }; } catch (error) { return { status: 'FAILED', items: [], fetchedPages: page, warnings: [], errors: [error instanceof Error ? error.message : 'PARSE_ERROR'] }; } }
